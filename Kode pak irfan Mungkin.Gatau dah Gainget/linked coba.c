#include <stdio.h>
#include <stdlib.h>

	struct sNode{
	char data;
	struct nowNode *nextPtr;
	};

typedef struct lsitNode

void instrunctions(void);
void printnow(NowNodeptr currentPtr);
int  empty(NowNodePtr *xPtr);
void insertnum(NowNodePtr *xPtr,char value);

int main(){
	
	
}
